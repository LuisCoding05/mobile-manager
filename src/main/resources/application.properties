spring.application.name=manager

# Ruta al ejecutable adb (por defecto se usa adb en PATH)
adb.path=adb

# Carpeta donde ejecutar scrcpy por defecto (cambiar según tu sistema)
scrcpy.path=/home/adrian/scrcpy

# Prefer cookie-based session tracking; prevents JSESSIONID in URL when cookies are enabled.
server.servlet.session.tracking-modes=cookie

# Helpful debugging options (enable during development only)
# server.error.include-stacktrace=always
logging.level.mobile.personal.manager=DEBUG
